[package]
name = "caddy"
version = "0.1.0"
edition = "2021"
description = "Caddy reverse proxy — orca service-backend"

[package.metadata.orca]
category = "networking"

[lib]
name = "caddy"
path = "src/lib.rs"
crate-type = ["rlib"]

[[bin]]
name = "caddy"
path = "src/main.rs"

# caddy is a BACKEND-ONLY orca plugin: zero #[orca_tool]s. It is a subprocess
# [[bin]] that talks to the orca daemon over its Unix socket via
# serve_service_plugin! from plugin-toolkit. No FFI/cdylib.
[dependencies]
plugin-toolkit = { git = "https://github.com/argyle-labs/orca", branch = "main" }

[dev-dependencies]
plugin-toolkit = { git = "https://github.com/argyle-labs/orca", branch = "main" }
tokio    = { version = "1", features = ["macros", "rt-multi-thread"] }
tempfile = "3"

[lints.clippy]
let_underscore_must_use = "deny"
